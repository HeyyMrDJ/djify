kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
# configure cluster with containerd registry mirror for the in-cluster registry
containerdConfigPatches:
- |-
  [plugins."io.containerd.grpc.v1.cri".registry]
    config_path = "/etc/containerd/certs.d"
nodes:
- role: control-plane
  kubeadmConfigPatches:
  - |
    kind: InitConfiguration
    nodeRegistration:
      kubeletExtraArgs:
        node-labels: "ingress-ready=true"
  extraPortMappings:
  - containerPort: 80
    hostPort: 80
    protocol: TCP
  - containerPort: 443
    hostPort: 443
    protocol: TCP
  extraMounts:
  - hostPath: ./registry-configs
    containerPath: /etc/containerd/certs.d
    readOnly: true
- role: worker
  extraMounts:
  - hostPath: ./registry-configs
    containerPath: /etc/containerd/certs.d
    readOnly: true
- role: worker
  extraMounts:
  - hostPath: ./registry-configs
    containerPath: /etc/containerd/certs.d
    readOnly: true
